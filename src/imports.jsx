let x=0;
while (x!=26)
{
    x+=1;
   /*
    y= ` <ParallaxLayer offset={o+=1} speed={v+=1}
    style={{
            backgroundImage: 'url(\${p${x}})',
            backgroundSize: 'cover',
          }} >
            <h1>Welcome to The Mirage by Jerr.</h1>
        </ParallaxLayer> `;
    console.log(y)
    */
   
}

Math.floor((Math.random()*26)+1);